{
  "name": "simpleticket-node",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && concurrently \"npm run watch\" \"npm run assets\" --names \"💻,📦\" --prefix name",
    "watch": "nodemon ./start.js --ignore public/",
    "assets": "sass --watch public/scss/index.scss:public/dist/index.css",
    "build": "rimraf dist/ && babel ./ --out-dir public/dist/ --ignore ./node_modules",
    "test": "jest",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "mongoose": "^5.8.11",
    "mongoose-mongodb-errors": "^0.0.2",
    "morgan": "~1.9.1",
    "node-sass-middleware": "0.11.0",
    "pug": "^2.0.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^3.0.1"
  }
}
